<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.simplifiedcoding.androidtablayout">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>  
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    
    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/shule_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="true">

        <!-- Home -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!--About Activity-->
        <activity
            android:name=".About"
            android:label="@string/about"
            android:theme="@style/ActivityTheme" />

        <!--  Settings Activity -->
        <activity
            android:name=".Settings"
            android:label="@string/settings"
            android:theme="@style/ActivityTheme"
            />



        <!--  Details Activity -->
        <activity android:name=".t_DetailActivity.Detail2Activity_t"
            android:label="@string/title_activity_news"
            android:theme="@style/ActivityTheme" />


        <!--Login Activity-->
        <activity
            android:name=".Login"
            />
        <!--Login Activity-->
        <activity
             android:label="@string/app_name"

            android:name=".result_codes.SingleItemView"
            />

        <!--Login Activity-->
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Account_Activity-->
        <activity
            android:name=".Accounts"
            android:theme="@style/MyDialogTheme"
            android:noHistory="true"
            android:label="Students" >
        </activity>


        <!--  Report Activity -->
        <activity
            android:name=".results.Report"
            android:label="@string/report"
            android:theme="@style/ActivityTheme"
            />

    </application>

</manifest>
